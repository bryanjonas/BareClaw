# Cron job configuration example.
# Copy this file to crons/<your-id>.yaml and customise.

id: my-cron

# Standard 5-field cron expression (minute hour day month weekday).
# Examples:
#   "* * * * *"   — every minute
#   "0 * * * *"   — every hour
#   "0 9 * * 1-5" — 09:00 on weekdays
schedule: "0 * * * *"

# Define exactly one target:
# 1) A scheduled project task.
project: my-project
task: check-system

# 2) Or an explicit shell command.
# command: "df -h"
# workspace: ~/workspace
# timeout: 30

# Send the agent's response to all allowed Telegram users.
notify_telegram: false
